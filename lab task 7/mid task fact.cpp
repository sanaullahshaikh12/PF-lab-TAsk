#include<stdio.h>
int main()
{
	int number,alarm=1,factor[60];
	scanf("%d",&number);
	forfactor= number;
	number= 0;
	for(int i = 0)	
		if( number%forfactor ==0 && number>1)
		{
		factor[i]= number;
		i++;
		}
	}
	
}
